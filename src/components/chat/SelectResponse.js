import React from 'react'

export default function SelectResponse(select) {
    return (
        <div>hello
            {/* {select.map((res) => (
                <input 
                                className="bot-responses" 
                                type="submit" 
                                value={res.text} 
                 />
            ))}
              */}
        </div>
    )
}
